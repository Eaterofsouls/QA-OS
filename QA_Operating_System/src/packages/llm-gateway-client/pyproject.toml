[project]
name = "llm-gateway-client"
version = "1.0.0"
description = "LLM gateway wrapper — the ONLY permitted path to model providers"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.0", "httpx>=0.27", "opentelemetry-api>=1.0"]

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-asyncio>=0.23"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]

[tool.hatch.build.targets.wheel.sources]
"." = "llm_gateway_client"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
